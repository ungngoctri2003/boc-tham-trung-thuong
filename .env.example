# PostgreSQL (bắt buộc khi deploy lên Vercel - SQLite không chạy trên serverless)
# Dùng Vercel Postgres hoặc Neon: https://vercel.com/docs/storage/vercel-postgres
# Ví dụ: postgresql://user:password@host/database?sslmode=require
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
JWT_SECRET="your-secret-key-change-in-production"
# User thường: chỉ đăng nhập + quay. Phân tách dấu phẩy hoặc xuống dòng.
# Ví dụ: ALLOWED_EMAILS="user1@dssolution.jp,user2@dssolution.jp"
ALLOWED_EMAILS=""
# Admin: đăng nhập + quay + vào trang admin (xem/sửa danh sách kết quả).
# Ví dụ: ADMIN_EMAILS="tri.ung@dssolution.jp"
ADMIN_EMAILS=""
# Chỉ email này (hoặc danh sách) mới thấy nút "Xóa hết & reset pool".
RESET_ADMIN_EMAILS="tri.ung@dssolution.jp"

# Xuất kết quả lên Google Sheet (tùy chọn)
# ID của Google Sheet (lấy từ URL: .../d/SHEET_ID/...)
# GOOGLE_SHEET_ID=""
# Đường dẫn tới file JSON Service Account (tạo từ Google Cloud Console)
# GOOGLE_APPLICATION_CREDENTIALS="credentials/google-service-account.json"